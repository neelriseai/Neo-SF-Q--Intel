# Canonical ontology and directional propagation review

## Scope

This review covers A1 canonical normalization and the isolated A2 direction/risk foundation. It
does not authorize live tools or release decisions. The governance 2.x interlock remains active.

## Independent challenge result

The genericity lane found no machine absolute paths, demo business constants or source vocabulary
in core reasoning. The governance lane found no false-release path. Both lanes required corrections
before approval: externally pin authority policies, bind source identity once, preserve missing
provenance, exclude unverified records from authoritative outputs, validate raw-relation endpoint
semantics, keep legacy runs readable, and reject forged graph/path/result identities. Those
corrections and adversarial tests are included in this slice.

## Current source truth

The configured Salesforce graph contains 1,012 nodes and 3,700 edges. All current kinds and
relations map with zero ontology/signature gaps. It has 19,071 record-level trust gaps and zero
confirmed nodes or edges because its producer predates explicit evidence envelopes. This is not
hidden or repaired with a whole-graph trust default: trusted traversal remains unavailable for that
source until its producer supplies honest record provenance. Therefore A1 remains `FOUNDATION`.

## A2 boundary

The isolated A2 engine has deterministic directional routes, relation-specific depth, ordered path
receipts, cycle/diamond/multi-seed handling, bounded enumeration, separate risk policy, explicit
missing-factor abstention and graph/path/policy tamper rejection. Every traversed node and edge must
match the trusted raw-graph digest, and risk evaluation independently binds the source graph,
ontology and source profile. It remains `FOUNDATION` because risk-factor observations and hop
freshness are not yet governed receipts and the assurance workflow does not consume A2 outputs yet.

## Verification expectation

The full repository gate must pass after regenerating `knowledge/project-index.json` and
`knowledge/application-graph.json`. Generated knowledge is discovery context, never authority.
