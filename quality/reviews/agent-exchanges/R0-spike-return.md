# R0 Spike Return

```json
{"q1":{"persisted":true,"where":"Embedded, not standalone: the full GraphProductionArtifact is serialized into the candidate assurance bundle document and stored in table candidate_assurance_bundles.bundle_document (Postgres and SQLite), keyed only by bundle_sha256 (content digest of the whole bundle). No entity index, no run key, no standalone file, nothing under .runtime/**. NOT CURRENT for healing use: it is written only when analyze_current_candidate runs; the artifact carries valid_until (<=3600s); and the live host DB .runtime/neo_sf_q_intel.db contains only table assurance_runs, so zero persisted copies exist on this host today.","evidence":"src/neo_sf_q_intel/candidate_assurance.py:644;src/neo_sf_q_intel/repository.py:137;src/neo_sf_q_intel/repository.py:322;src/neo_sf_q_intel/service.py:887"},"q2":{"inputs":["project_id (bounded logical id)","git repository root (SALESFORCE_REPOSITORY_ROOT)","sfdx app root (SALESFORCE_APP_ROOT, must be inside the git root)","config/foundation-pipeline-policy.json","config/verified-change-policy.json","config/graph-producer-policy.json","config/operation-seed-policy.json","config/ontology/canonical-ontology.json","config/source-profiles/salesforce-dx-semantic-graph.json","self-digests of src/neo_sf_q_intel/graph_production.py, salesforce_graph_adapter.py, graph_adapter.py, ontology.py","git plumbing reads: rev-parse --show-toplevel, tracked plus nonignored-untracked manifest, batched blob reads for BOTH full BASE and CANDIDATE trees","no caller-supplied git refs or paths: scope is host-owned and caller scope is refused"],"readOnly":true,"evidence":"src/neo_sf_q_intel/foundation_pipeline.py:388;src/neo_sf_q_intel/graph_production.py:1257;src/neo_sf_q_intel/graph_production.py:1277;src/neo_sf_q_intel/graph_production.py:1239"},"q3":{"measured":true,"ms":18535,"why":"Measured on this host against the configured real project ../SalesForceAgentApp/strategic-deal-assurance via CandidateFoundationPipeline.from_host_configuration(...).capture_current(). Cold run 18535 ms; two warm in-process repeats 18427 ms and 19139 ms; policy composition 23 ms. verify_current(captured) is a FULL re-capture and cost 19752 ms, so the path service.analyze_current_candidate actually uses (capture plus verify) is about 38 s. Result shape: 388 BASE nodes, 402 CANDIDATE nodes, 17 delta entries."},"q4":{"singleEntityPossible":false,"why":"No. The Salesforce adapter extracts over the entire side content map in one pass, and a single node id is merged across every owning file (attributes and owner_paths accumulate, evidence_state can be promoted INFERRED to CONFIRMED), so an entity semantic_sha256 and its owner path set are only well defined after the whole tree is walked. _make_delta then diffs full BASE/CANDIDATE element maps, and the GraphProductionArtifact validator recomputes the entire delta and rejects any artifact whose delta differs. A per-entity shortcut would have to guess owner paths, would yield a different semantic digest, and would be exactly the silent degradation the repo forbids. Cheap CORRECT single-entity lookup is possible only as an index read over an already-produced artifact.","evidence":"src/neo_sf_q_intel/graph_production.py:1428;src/neo_sf_q_intel/salesforce_graph_adapter.py:285;src/neo_sf_q_intel/graph_production.py:867;src/neo_sf_q_intel/graph_production.py:454"},"q5":{"nodeIdGrammar":"CONFIRMED. Shape is kind:identity. Fields are exactly field:{ObjectApiName}.{FieldApiName}, built as full_name = {object_name}.{field_name} then builder.node(field:{full_name}, field, full_name, path, ...), so field:Opportunity.Regional_VP_Approver__c is the real emitted node_id. The producer passes adapter node ids through verbatim (no ontology rewrite of node_id). Other kinds observed: object:{Name}, apex:{ClassName}, trigger:{Name}, lwc:{Name}, flow:{Name}, page:{Name}, app:{Name}, tab:{Name}, layout:{Name}, approval:{Name}, permission-set:{Name}, report:{path}, report-folder:{Name}, list-view:{Object}:{Name}, field-update:{Object}.{Name}, component-test:{path}. The producer additionally injects one file:{repo-relative-posix-path} node per manifest entry. Edge ids are edge:{first 32 hex of a stable digest} and are NOT human-readable.","evidence":"src/neo_sf_q_intel/salesforce_graph_adapter.py:649;src/neo_sf_q_intel/salesforce_graph_adapter.py:651;src/neo_sf_q_intel/graph_production.py:1577;src/neo_sf_q_intel/graph_production.py:1520;src/neo_sf_q_intel/salesforce_graph_adapter.py:343"},"q6":{"gates":["Policy root pin: policy.sha256 must equal DEFAULT_GRAPH_PRODUCER_POLICY_SHA256 else POLICY_ROOT_MISMATCH","Producer self-digest pin: sha256 of graph_production.py itself (with the digest constant blanked) must match policy.producer.implementationSha256 - editing that file fails production closed until config/graph-producer-policy.json is re-pinned","Adapter, adapter-contract and normalizer locator plus digest pins (salesforce_graph_adapter.py, graph_adapter.py, ontology.py)","Verified-change-policy, ontology and source-profile contract pins (id plus version plus sha256)","Foundation pipeline policy cross-pin of the three dependency policy digests","Repository root identity gate: git rev-parse --show-toplevel must equal the configured expected root","Freshness authority: observed_at/valid_until with freshnessSeconds 3600, minimised against upstream authority validity; expired capture fails closed (CAPTURE_EXPIRED)","Receipt digests: receipt_sha256, side_receipt_sha256 and every element/semantic/delta digest are re-verified on model validation","Authority scope: authority_scope=ANALYSIS_ONLY, release_eligible=False, permanent blocking_gap_codes cannot be omitted","Capability scope registry: config/capability-scope.json entry source.tree-graph-production must list implementation plus verification files and five verification-case classes (positive, negative, failure, degradation, scenarioIndependence)","MCP surface gate: _NeoMCPServer rejects input for zero-scope tools and failures must surface as sanitized ToolError codes"],"evidence":"src/neo_sf_q_intel/graph_production.py:1141;src/neo_sf_q_intel/graph_production.py:1145;src/neo_sf_q_intel/graph_production.py:1153;src/neo_sf_q_intel/graph_production.py:1193;src/neo_sf_q_intel/foundation_pipeline.py:435;src/neo_sf_q_intel/graph_production.py:1239;config/graph-producer-policy.json:59;src/neo_sf_q_intel/graph_production.py:429;config/capability-scope.json:38;src/neo_sf_q_intel/mcp_server.py:17"},"options":[{"opt":"A","cost":"About 18.5-19.1 s per tool call for capture only; about 38 s if the tool follows the repo capture-plus-verify replay convention as service.analyze_current_candidate does. Measured, not estimated.","risk":"Unusable inside a browser healing window: every entity lookup re-reads both full trees via git and re-runs the adapter. Repeated full-tree work, likely MCP or browser timeouts, and a healing loop that asks about several entities pays the cost N times."},{"opt":"B","cost":"One production per healing run (about 19 s capture, about 38 s if verify-replayed), then sub-millisecond dict lookups for every subsequent entity_id. Zero marginal cost per entity.","risk":"Process-local only - no reuse across API worker processes or a restarted MCP server, and each new process pays the first-call cost. Staleness is the real hazard and is bounded by construction: key the cache on (repository_identity_sha256, verified_change_manifest_sha256) and hard-expire at artifact.valid_until (<=3600 s), failing closed with an explicit reason code rather than serving stale evidence. Low blast radius: an additive accessor on the service, no change to foundation_pipeline, repository or the bundle write path."},{"opt":"C","cost":"Near-zero extra production cost (the artifact is already assembled in-memory during the pipeline run); adds a write plus a read path.","risk":"High blast radius on the exact code the platform evidence authority rests on: the bundle persistence path enforces digest-exact, immutable, single-bundle-per-component-run invariants, and the bundle is written only when the full analyze path runs. The persisted copy is keyed by bundle_sha256 with no entity index and no currency guarantee, so a healing tool would routinely read evidence past its valid_until - a no-silent-degradation violation. The live host SQLite DB does not even carry candidate_assurance_bundles today, so this option also requires a migration before it works at all."},{"opt":"D","cost":"Schema migration plus write path plus index plus governance entries, and it still needs a full 19 s production run to fill each row.","risk":"Highest. It creates a second authority for derived evidence that can diverge from delta_sha256 and receipt_sha256 with no re-validation on read, denormalizes receipt-verified data into rows, adds migration and rollback surface to a repository layer whose current invariants are digest-exactness and immutability, and buys nothing over B for a single healing window."}],"recommend":{"opt":"B","why":"Correctness: it serves entries straight off a receipt-validated GraphProductionArtifact, so operation, entityType and both owner path sets come from the same _make_delta the artifact validator recomputes - no reimplementation, no second authority, no guessed owner paths. Latency: measured 18.5-19.1 s once per healing run, then effectively free per entity, which is the only one of the four options that fits a browser healing window without either a 19-38 s stall per lookup (A) or a migration (C, D). Blast radius: an additive read-only accessor plus cache on AssuranceService and two MCP tools; graph_production.py and salesforce_graph_adapter.py are NOT touched, which matters because both are self-digest-pinned and editing either fails production closed. No-silent-degradation: the cache is keyed on (repository_identity_sha256, verified_change_manifest_sha256) and hard-expires at artifact.valid_until, so a stale or mutated tree produces an explicit reason code and a re-capture, never a quietly wrong answer; an entity_id miss returns operation=none from a known-complete artifact rather than from an absent one. C remains the right follow-on once an entity-indexed, currency-keyed persistence contract is actually specified; it is not a healing-window prerequisite."},"r1EstimateMinutes":90,"r1Risks":["R1 IS LARGER THAN 30m. 30 minutes is unrealistic; the honest range is 75-110 minutes, centred on 90.","Functional tests that exercise real production cost 18.5-38 s of wall clock EACH on the configured project, so a credible functional suite needs a synthetic fixture git repo in the tests/test_foundation_pipeline.py style; building that fixture is itself 20-30 minutes of the estimate.","change_neighborhood is not a thin wrapper: it needs bounded hop traversal over ProducedGraphSide.edges with deterministic ordering, hop and maximum_edges limits, a new bounded response model, and a decision on which side (BASE, CANDIDATE or both) it traverses - that side-selection contract is a design question, not a coding one.","AGENTS.md governance requires five verification-case classes (positive, negative, failure, degradation, scenarioIndependence) per capability and config/capability-scope.json must gain or extend an entry: roughly 10 test cases across the two tools before promotion is legitimate.","The cache invalidation contract (valid_until expiry, manifest-digest change, FoundationPipelineUnavailable) each needs its own failing-closed test with an explicit reason code; this is the no-silent-degradation surface and cannot be skipped.","graph_production.py and salesforce_graph_adapter.py are self-digest-pinned: any accidental edit, even formatting, fails production closed with PRODUCER_IMPLEMENTATION_MISMATCH until config/graph-producer-policy.json is re-pinned. The accessor must live in service.py or a new module.","tests/test_mcp_server.py builds services with no foundation pipeline configured, so a functional test for these two tools needs a new fixture path that wires a real or synthetic pipeline into build_mcp.","Decide up front whether the tool produces via capture only (19 s) or capture plus verify (38 s). The existing convention on the analyze path is capture plus verify; choosing capture-only is a deliberate, documentable relaxation, not a silent one."],"blockers":["NON-BLOCKING FINDING, OUT OF SCOPE: the live host SQLite store .runtime/neo_sf_q_intel.db contains only table assurance_runs and is missing candidate_assurance_bundles from SQLITE_SCHEMA_SQL, so no candidate bundle - and therefore no persisted GraphProductionArtifact - exists on this host today. This invalidates options C and D until a migration runs.","SECURITY, OUT OF SCOPE, OBSERVED WHILE READING CONFIG: the untracked .env at the repository root holds a live plaintext OPENAI_API_KEY and a plaintext DATABASE_URL password. Not reproduced here. Recommend rotating the key."]}
```
