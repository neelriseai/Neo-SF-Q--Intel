# G3 return

```json
{"files":[{"path":"src/neo_sf_q_intel/element_signature.py","action":"modified","lines":435},{"path":"tests/test_element_signature.py","action":"modified","lines":356},{"path":"tests/test_element_signature_functional.py","action":"modified","lines":330}],"tests":{"added":5,"tightened":5,"passed":89,"skipped":11,"failed":0},"redRunConfirmed":true,"callSitesAffected":[{"file":"src/neo_sf_q_intel/element_signature.py","line":317,"behaviourChange":"none - `except ElementSignatureError: raise` in _signature_from_row still re-raises SignatureCorrupt unchanged because SignatureCorrupt remains a subclass of the base"},{"file":"tests/test_element_signature_functional.py","line":268,"behaviourChange":"tightened by this chunk from ElementSignatureError to SignatureCorrupt (4 tamper tests + 1 unreachable-store negative)"}],"defectsFound":[{"sev":"high","desc":"corruption raised the BASE ElementSignatureError so no dedicated type existed for tamper; a caller catching SignatureStoreUnavailable never caught it","evidence":"pre-fix probe: _signature_from_row(('a','b')) -> class=ElementSignatureError code=SIGNATURE_CORRUPT caught_by_StoreUnavailable=False"},{"sev":"medium","desc":"G1 functional test test_a_tampered_row_is_also_refused_through_the_obligation_index mutated captured_at_utc without reading the value back, so it would have passed had the UPDATE silently no-opped on a different value","evidence":"pre-fix file line 314: only `assert _mutate(...) == 1`, no _column() read-back and no inequality check against the stored signature"},{"sev":"low","desc":"no production call site anywhere in the repo catches SignatureStoreUnavailable or ElementSignatureError; the distinction is currently only consumed by tests","evidence":"repo-wide grep for `except (SignatureStoreUnavailable|ElementSignatureError|SignatureCorrupt)` returns exactly one hit, inside element_signature.py itself"}],"deviations":[{"from":"STATE_TRANSITIONS rows 5-8","why":"added a 5th unit test (test_a_short_row_is_refused_as_signature_corrupt_not_store_unavailable) that drives a truncated row through the fake connection, so the raise-site conversion is proven without requiring PostgreSQL"},{"from":"REQUIRED_FIX item 2 literal wording","why":"raise sites now read `raise SignatureCorrupt(msg)` with the explicit `code=\"SIGNATURE_CORRUPT\"` kwarg dropped; the class attribute supplies the identical string, verified at runtime as SignatureCorrupt('x').code == 'SIGNATURE_CORRUPT'"},{"from":"ANTI_VACUOUS","why":"strengthened row 4 beyond the brief: added the tampered != signature.captured_at_utc inequality and a _column() read-back, matching rows 1-3"}]}
```
