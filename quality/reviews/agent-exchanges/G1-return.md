# G1 return

```json
{"files":[{"path":"tests/test_element_signature_functional.py","action":"extend","lines":319},{"path":"tests/test_locator_healing_bridge_functional.py","action":"create","lines":234}],"tests":{"added":11,"passed":15,"skipped":15,"failed":0},"redRunConfirmed":true,"skipWithoutEnvVarConfirmed":true,"defectsFound":[{"sev":"low","req":"REQ-03","desc":"SIGNATURE_CORRUPT is raised as the base ElementSignatureError, not SignatureStoreUnavailable as the brief's AST contract states; a caller catching SignatureStoreUnavailable per that contract would let a tampered-row refusal escape uncaught. Behaviour is otherwise correct: the code is SIGNATURE_CORRUPT and no row is ever returned. src/ left unchanged.","evidence":"src/neo_sf_q_intel/element_signature.py:269-278 raises ElementSignatureError(code='SIGNATURE_CORRUPT'); lookup() calls _signature_from_row at line 372, outside the _session() context manager that would have wrapped it into SignatureStoreUnavailable. Tests assert ElementSignatureError + code."}],"deviations":[{"from":"REQ-03 State_Expected 'lookup() raises SignatureStoreUnavailable code SIGNATURE_CORRUPT'","why":"Asserted ElementSignatureError with code == 'SIGNATURE_CORRUPT' instead, because that is what the real source raises; asserting the narrower class would have made a passing implementation look broken. Recorded as a low-severity contract finding rather than fixed."},{"from":"existing fixture shape in tests/test_element_signature_functional.py","why":"Split the single `repository` fixture into `connection_factory` + `repository` so the REQ-03 tests can issue the direct SQL mutation through the same real connection. No behaviour change to the four pre-existing tests, which still pass."},{"from":"'mutate at least two distinct columns'","why":"Covered four: structure, attrs_hashed, signature_sha256, and captured_at_utc through lookup_by_obligation, so the refusal is proven on the secondary read path too."}],"minutesPerReq":{"REQ-02":18,"REQ-03":20,"REQ-13":16}}
```
